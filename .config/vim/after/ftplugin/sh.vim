setlocal shiftwidth=4 " sw, number of spaces for each (auto)indent
setlocal tabstop=4 " ts, number of spaces that a tab counts for
setlocal softtabstop=4 " sts, numbeer of spaces that a tab counts for while editing (?)
setlocal expandtab " et, convert tabs to spaces
